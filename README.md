# stat405
Final Project for STAT 405 (Bayesian Statistics). 

## Data
Run `data.R` from project root to generate `crash_weather.csv`. From R shell we can run:

```r
source("data.R")
```

Or we can run the following in terminal (bash):
```bash
Rscript data.R
```
